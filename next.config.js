/** @type {import('next').NextConfig} */

/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '',
    output: 'export',
  distDir: 'build',
  trailingSlash: true,
  reactStrictMode: true,
};

module.exports = nextConfig;
